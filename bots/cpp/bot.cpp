#include <cstdio>
#include <endofcodes.h>

using namespace std;

vector< Intent > getRound(vector< Creature > map) {
}

int main() {
    EndOfCodes eoc = EndOfCodes(getRound);
    return eoc.play();
}
